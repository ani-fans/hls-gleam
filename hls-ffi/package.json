{
  "name": "hls-ffi",
  "version": "1.0.0",
  "type": "module",
  "exports": {
    ".": {
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "rslib build && cp dist/index.js ../src/hls-ffi.mjs",
    "check": "biome check --write",
    "dev": "rslib build --watch",
    "format": "biome format --write",
    "test": "vitest run"
  },
  "devDependencies": {
    "@biomejs/biome": "2.1.3",
    "@rslib/core": "^0.12.0",
    "vitest": "^3.2.4"
  },
  "private": true,
  "dependencies": {
    "hls.js": "^1.6.9"
  },
  "trustedDependencies": [
    "core-js"
  ]
}
